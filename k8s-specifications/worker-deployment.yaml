apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app: worker
  namespace: dev 
  name: worker
spec:
  replicas: 1
  selector:
    matchLabels:
      app: worker
  template:
    metadata:
      labels:
        app: worker
    spec:
      containers:
      - image: dockersamples/examplevotingapp_worker
        name: worker
