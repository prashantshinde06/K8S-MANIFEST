apiVersion: v1
kind: Service
metadata:
  name: cluster-ip-service-for-my-web-app
spec:
  type: ClusterIP
  selector:
    app: web-app
  ports:
    - name: http
      port: 8080 # Service port (inside cluster)
      targetPort: 3000 # Pod container port
